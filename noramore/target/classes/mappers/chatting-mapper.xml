<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="chatting">

   <resultMap type="Message" id="chatRequest">
      <result property="messageContent" column="message_Content" />
      <result property="sender" column="sender" />
      <result property="reciever" column="reciever" />
      <result property="connectYN" column="connectYN" />
   </resultMap>

   <resultMap type="Member" id="member_rm">
      <id property="memberID" column="MEMBER_ID"/>
     
      <result property="photoFilename" column="PICTURE_FILE"/>
   </resultMap>   


   <!--=========================================================================================-->

   <!-- 채팅 요청 삽입 -->
<!--    <insert id="insertMessage" parameterType="Message">
      INSERT INTO MESSAGE
      VALUES(#{textMessage}, #{sender}, #{receiver), #{ connectYN }, sysdate)
   </insert> -->
   
<!--    <select id="selectChatRequest" parameterType="string" resultType="chatRequest">
   		select max(regist_date)
   		from message
   		where receiver = #{memberID}
   </select> -->

</mapper>